document.addEventListener('DOMContentLoaded', function() {

  var group_select = document.querySelector('select[name="group"]');

  // // Init Materialize CSS for group select
  M.FormSelect.init(group_select, {});

});
